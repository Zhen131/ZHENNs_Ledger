# models

This directory defines the core data structures, including `DecimalString`, `Asset`, `Trade`, `TradeDraft`, `PriceSnapshot`, `FeeRule`, `Position`, and `LedgerData`.

Models define data shape only. They do not calculate positions, validate untrusted input, persist data, or render UI.
