# utils

This directory contains small utilities shared across layers.

`decimalMath.ts` centralizes decimal conversion, arithmetic, comparison, tolerance checks, and formatting. Date utilities centralize strict ledger date keys and one-clock-snapshot business operations.

This directory is not a catch-all. Utilities with a clear business owner belong in that layer.
